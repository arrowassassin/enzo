[package]
name = "quire-catalog"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Builds the Bookshop catalog (bookshop/catalog.bin, the device's fixed-record format) from the curated bookshop/catalog.json"

[dependencies]
quire-ui = { path = "../../crates/quire-ui", default-features = false, features = ["std"] }
quire-fs = { path = "../../crates/quire-fs", features = ["std"] }
serde_json = { workspace = true }
anyhow = { workspace = true }
clap = { workspace = true }
